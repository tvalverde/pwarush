# Refuse to install under the wrong Node/npm so the toolchain stays on the
# version pinned in .nvmrc (see root package.json "engines"). This catches Node
# 25 (leaks globals that break the test suite) and Node 20.x (crashes the
# toolchain) before they cause confusing failures. Use `make setup`/nvm to align.
engine-strict=true
